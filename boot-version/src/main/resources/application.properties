# Spring Boot Application Configuration
spring.application.name=mcp-plantuml-boot

# Server Configuration
server.port=8080

# MCP Server Configuration
mcp.server.name=plantuml-mcp-server
mcp.server.version=1.0.0

# Logging
logging.level.root=INFO
logging.level.ai.gateway=DEBUG
